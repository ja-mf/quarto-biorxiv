$if(shorttitle)$
\shorttitle{$shorttitle$}
$else$
\shorttitle{$title$}
$endif$
$if(leadauthor)$
\leadauthor{$leadauthor$}
$else$
$for(by-author/first)$
$if(by-author.name.family)$
\leadauthor{$by-author.name.family$}
$else$
\leadauthor{$by-author.name.literal$}
$endif$
$endfor$
$endif$

% Footer options (default: show both logo and date)
% Use show-logo: false or show-date: false in YAML to hide them
$if(hide-logo)$
\showlogofalse
$endif$
$if(hide-date)$
\showdatefalse
$endif$

$if(title)$
\title{$title$}
$endif$

$for(by-author)$
\author[$for(by-author.affiliations)$$by-author.affiliations.number$$sep$,$endfor$$if(by-author.attributes.corresponding)$,\Letter$endif$]{$if(by-author.name.literal)$$by-author.name.literal$$else$$by-author.name.given$ $by-author.name.family$$endif$$if(by-author.orcid)$ \orcidlink{$by-author.orcid$}$endif$}
$endfor$

$for(by-affiliation)$
\affil[$by-affiliation.number$]{$by-affiliation.name$}
$endfor$

\date{}
